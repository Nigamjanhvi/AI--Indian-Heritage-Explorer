# Jenkins

The root `Jenkinsfile` includes the requested pipeline stages:

1. Git Clone
2. Install Dependencies
3. Run Tests
4. Build Frontend
5. Build Backend
6. Docker Build
7. Docker Compose Deployment
8. Health Check
